//interface are more restricted than abstract class
//we cannot create concrete methods 
//interfaces are going to define contract or specification which is going to govern how application or code or programming logic behave.

package Interface;

interface Bicycle {
	public void Brake(int decrement);
	public void SpeedUp(int increment);
	
}
